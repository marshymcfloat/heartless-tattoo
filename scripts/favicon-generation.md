# Favicon source

Created with the built-in image-generation tool from the supplied monogram reference.
Master artwork: `public/brand/heartless-monogram.png`.
Run `node scripts/generate-favicons.mjs` to regenerate the Next.js icon files.

## Initial prompt

Edit the supplied reference image into a favicon for Heartless Tattoo. Preserve the exact ornate intertwined calligraphic monogram, its strokes, flourishes, proportions, and centered circular composition; do not redesign or substitute a font. Change only the treatment: pure white monogram on a solid black circular disc, with transparent pixels outside the disc. No metallic shading, gradients, extra text, or new elements. Output one crisp square 1024×1024 PNG suitable for favicon downscaling, with the circle nearly filling the canvas and the complete monogram inside.

## Final correction prompt

Edit this generated favicon image. Change ONLY the checkerboard outside the black circular disc to a completely solid pure white (#FFFFFF) background. Keep the black disc and the existing white ornate monogram exactly unchanged, including every flourish, stroke, shape, size, and position. No checkerboard, no transparency simulation, no gray texture. Square black-and-white favicon image.
